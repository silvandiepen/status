{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://plugins.status.app/schemas/plugin/v1/views.schema.json",
  "title": "Status plugin views (views.json)",
  "description": "Which built-in app-owned view types the plugin uses and what data feeds them. Plugins never render custom UI; the app renders every view natively.",
  "type": "object",
  "additionalProperties": false,
  "required": ["views"],
  "properties": {
    "views": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/view" }
    },
    "dashboardTile": {
      "$ref": "#/$defs/dashboardTile"
    }
  },
  "$defs": {
    "dashboardTile": {
      "type": "object",
      "description": "Recommended dashboard tile fields for newly configured apps. Status renders the tile natively and lets users override these choices per app.",
      "additionalProperties": false,
      "properties": {
        "primaryFields": {
          "type": "array",
          "description": "Most important normalized resource fields to show first on the compact dashboard tile.",
          "maxItems": 2,
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "secondaryFields": {
          "type": "array",
          "description": "Additional normalized resource fields to show after the primary field, up to the app-owned tile limit.",
          "maxItems": 4,
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        }
      }
    },
    "view": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_-]*$"
        },
        "type": {
          "enum": [
            "overview_cards",
            "resource_list",
            "resource_detail",
            "timeline",
            "metric_grid",
            "alert_list"
          ]
        },
        "title": {
          "type": "string",
          "minLength": 1
        },
        "resourceType": {
          "type": "string",
          "description": "Resource type feeding this view. Required for resource_list and resource_detail.",
          "pattern": "^[a-z][a-z0-9_]*$"
        },
        "fields": {
          "type": "array",
          "description": "Normalized resource fields to show, in order.",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "type": { "enum": ["resource_list", "resource_detail"] } }
          },
          "then": {
            "required": ["id", "type", "resourceType"]
          }
        }
      ]
    }
  }
}
