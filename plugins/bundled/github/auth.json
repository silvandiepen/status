{
  "type": "bearer-token",
  "provider": "github",
  "applicationId": "status-foundry.github",
  "fields": [
    {
      "key": "token",
      "label": "Personal access token",
      "type": "secret",
      "placeholder": "github_pat_...",
      "help": "Create a fine-grained GitHub token for this repository with selected-repository access and read-only Metadata, Actions, Pull requests, and Issues permissions. Add Contents read-only if GitHub requires it for activity reads. Status stores the token in Keychain.",
      "required": true
    }
  ]
}
