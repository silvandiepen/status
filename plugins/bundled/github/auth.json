{
  "type": "bearer-token",
  "fields": [
    {
      "key": "token",
      "label": "Personal access token",
      "type": "secret",
      "placeholder": "github_pat_...",
      "help": "Use a fine-grained read-only token for the repository.",
      "required": true
    }
  ]
}
