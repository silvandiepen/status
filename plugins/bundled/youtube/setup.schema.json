{
  "title": "YouTube creator account",
  "description": "Connect a Google account with access to the YouTube channel you want Status to watch.",
  "fields": [
    {
      "key": "_status.oauth.clientId",
      "label": "Google OAuth client ID",
      "type": "text",
      "placeholder": "000000000000-example.apps.googleusercontent.com",
      "help": "Create an OAuth client in Google Cloud for the Status redirect URI status://oauth/youtube. Paste the public client ID here before connecting.",
      "required": true
    }
  ]
}
